import type { Narration } from "../../registry/types";

export const narrations: Narration[] = [
  "电脑上敲一行指令，linear.x 0.2 发出去，小车就往前跑。轮子是怎么听懂的？没人碰过它一下。",
  "这中间隔着好几层：一条叫 Twist 的消息，一个差速运动学公式，一个指令队列，一套 PID 闭环。少任何一层，轮子都不转。",
  "上一章我们看了相机画面，这一章往里走，进小车的神经系统。从一条指令到轮子转动，整条链路拆开看，一个环节都不跳。",
];
